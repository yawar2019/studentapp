import { ChangCasePipe } from './chang-case.pipe';

describe('ChangCasePipe', () => {
  it('create an instance', () => {
    const pipe = new ChangCasePipe();
    expect(pipe).toBeTruthy();
  });
});
